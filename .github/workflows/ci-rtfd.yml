name: RTFD Preview
on:
  pull_request_target:
    types:
      - opened

permissions:
  pull-requests: write

jobs:
  documentation-links:
    runs-on: self-hosted
    steps:
      - uses: readthedocs/actions/preview@v1
        with:
          project-slug: "pytorch-lightning"
